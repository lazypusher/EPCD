# 每模板 TPE 最优设置（9 模板 540 轮实测，两档分类）
#
# ⚠ 本文档是【只读参考】，不再是生产代码读取的源。
# 生产代码真正的单一来源（single source of truth）是：
#   backend/src/epcd_agent/optimizer/tpe_settings.py （_TPE_TABLE）
# 改模板的 startup_trials / max_rounds 请改上面那个模块，并同步此 toml 保持一致。
# 解析优先级（在 optimize_tools.optimization_start 中实现）：
#   显式参数 > 每模板表 > 内置默认（startup_trials=5, max_rounds=15）

["system.inductor.simple_inductor"]
startup_trials = 5
max_rounds = 15

["system.inductor.adv_simple_inductor"]
startup_trials = 5
max_rounds = 15

["system.inductor.bowtie_inductor"]
startup_trials = 5
max_rounds = 10

["system.inductor.stack_inductor"]
startup_trials = 10
max_rounds = 20

["system.inductor.differential_inductor"]
startup_trials = 10
max_rounds = 20

["system.inductor.differential_inductor_step"]
startup_trials = 10
max_rounds = 20

["system.inductor.stack_inductor_overlapped"]
startup_trials = 10
max_rounds = 20

["system.tcoil.tcoil_inductor_oct"]
startup_trials = 10
max_rounds = 20

["system.tcoil.tcoil_inductor_rec"]
startup_trials = 5
max_rounds = 15