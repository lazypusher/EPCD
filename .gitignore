# 运行产物
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
platform/server/data/

# git 历史备份
.git_backups/

# Node / 前端
node_modules/
dist/
*.log
.DS_Store

# EPCD 统一运行产物根（所有任务的中间/交付产物，不入库）
# 结构：runs/epcd-agent-session.sqlite3（全局审计库）+ runs/<任务名>/artifacts|opt-input
runs/
*.opt-input-*.json
# EPCD �Ż������м����
opt-input.json
